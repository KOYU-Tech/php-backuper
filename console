<?php
require __DIR__ . '/functions.php';
require __DIR__ . '/vendor/autoload.php';

use App\RegisterCommands;

if(isset($argv[1])) {
    $registry = new RegisterCommands($argv[1]);
} else {
    echo 'Необходимо передать название команды';
}


